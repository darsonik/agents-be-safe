[project]
name = "agents-be-safe"
version = "0.1.0"
description = "Read-only security assessment of agent skills and MCP tools using Fireworks and Jev"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
